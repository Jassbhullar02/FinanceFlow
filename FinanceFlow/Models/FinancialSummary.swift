import Foundation

// Holds the totals shown in the home dashboard hero and income/outcome strip.
struct FinancialSummary {
    let balance: Decimal
    let income: Decimal
    let outcome: Decimal
}
